apiVersion: v1
kind: Pod
metadata:
   name: toleration1
   labels:
     purpose: "toleration1-demo"
spec:
    nodeSelector:
      topology.kubernetes.io/zone: us-east-1d
    containers:
    - name: nginx 
      image: nginx