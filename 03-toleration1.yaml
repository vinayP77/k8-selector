apiVersion: v1
kind: Pod
metadata:
   name: tolerations
   labels:
     purpose: "tolerations-demo"
spec:
    nodeSelector:
      topology.kubernetes.io/zone: us-east-1d
    containers:
    - name: nginx 
      image: nginx